import React from 'react';

// 에러 방지용 임시 컴포넌트입니다.
export const TagCreator = () => {
  return (
    <div className="p-4 border border-dashed border-gray-400">
      태그 생성기 (TagCreator) 컴포넌트가 없습니다.
    </div>
  );
};